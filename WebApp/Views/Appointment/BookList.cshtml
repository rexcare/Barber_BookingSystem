@model IEnumerable<App.Domain.Appointment>

@using Microsoft.AspNetCore.Identity
@using App.Domain.Identity
@using App.Domain
@using Base.Extensions
@inject SignInManager<AppUser> _signInManager
@inject UserManager<AppUser> _userManager

@{
    ViewData["Title"] = "Index";
    var index = 0;
}

<h1>Our services</h1>
<div class="table-responsive">
    <table class="table table-bordered table-striped table-hover">
            <thead>
                <tr>
                    <th>
                        No
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Customer)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.StartTime)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Service)
                    </th>
                    <th>
                        Price
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    var customer = (Customer?)item.Customer;
                    index++;
                    <tr>
                        <td>
                            @index
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Customer!.FirstLastName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.StartTime)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Service!.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Service!.Price)
                        </td>
                        <td>
                            <div class="form-group">
                        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#a-@item.Id">
                            View Detail
                            </button>
                            <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#b-@item.Id">
                                Cancel Appointment
                            </button>
                            </div>
                        </td>
                    </tr>

                }
            </tbody>
        </table>
        </div>
@foreach (var item in Model)
{
    var customer = (Customer?)item.Customer;
    <!-- Modal -->
    <div class="modal fade" id="a-@item.Id" tabindex="-1" aria-labelledby="modalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-centered modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="staticBackdropLabel">Confirm</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div style="display: flex; justify-content: center; align-items: center; flex-direction: column;">
                        <h5 class="text-primary">@item.Id</h5>
                        <p>@item.ServiceId</p>
                        <div class="card border-primary mb-3" style="min-width: 16rem; height: 100%; width: 100%">
                            <div class="card-body text-primary" style="display:flex">
                                <div class="calendar-icon">
                                    <a asp-action="index" class="text-secondary" style="font-size:100px; text-decoration: none;"><i class="fa fa-light fa-calendar"></i></a>
                                </div>
                                <div class="w-100">
                                    <figure>
                                        <blockquote class="blockquote">
                                            <p style="text-transform: capitalize;">name</p>
                                            <p id="bookTime">April 20, 2023 at 10:00 am</p>
                                        </blockquote>
                                        <figcaption class="blockquote-footer">
                                            <p>Description</p>
                                            <p>TimeSpan</p>
                                            <p>Price</p>
                                        </figcaption>
                                    </figure>
                                    <hr />
                                    <div class="w-50" style="float: right">
                                        <div class="space-between">
                                            <p>Subtotal</p>
                                            <p>$Price</p>
                                        </div>
                                        <div class="space-between">
                                            <p>Sales Tax</p>
                                            <p>$0</p>
                                        </div>
                                        <hr />
                                        <div class="space-between">
                                            <p>Total</p>
                                            <p>$Price</p>
                                        </div>
                                    </div>
                                </div>

                            </div>
                        </div>
                    </div>

                </div>
                <div class="modal-footer" style="display: flex; justify-content: center; flex-wrap: nowrap">
                    <button type="button" class="btn btn-primary w-50">OK</button>
                    <button type="button" class="btn btn-danger w-50">Cancel appointment</button>
                </div>
            </div>
        </div>
    </div>
    <div class="modal fade" id="b-@item.Id" tabindex="-1" aria-labelledby="modalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-centered modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="staticBackdropLabel">Confirm</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div style="display: flex; justify-content: center; align-items: center; flex-direction: column;">
                        <h5 class="text-primary">Confirm delete</h5>
                    </div>

                </div>
                <div class="modal-footer" style="display: flex; justify-content: center; flex-wrap: nowrap">
                    <a 
                        class="btn btn-danger w-50" 
                        asp-action="DeleteAppointment" 
                        asp-route-id="@item.Id" 
                        asp-route-uid="@User.GetUserId()" 
                    >
                        Cancel Appointment
                    </a>
                    <button type="button" class="btn btn-info w-50">Close</button>
                </div>
            </div>
        </div>
    </div>
}

<script>
    function toggleModal() {
        var myModal = new bootstrap.Modal(document.getElementById('modal'), {
            keyboard: false
        });

        myModal.toggle();
    }
    
    function viewDetail(id) {
        console.log(model[0])
        /*
        let data = {
            id: id
        }
        $.ajax({
            type: 'POST',
            url: '/Appointment/DetailAppointment',
            contentType: 'application/x-www-form-urlencoded; charset=UTF-8', // when we use .serialize() this generates the data in query string format. this needs the default contentType (default content type is: contentType: 'application/x-www-form-urlencoded; charset=UTF-8') so it is optional, you can remove it
            data: data,
            success: function (result) {
                if (result == "error") {
                    alert('Failed to reserve the Data');
                    console.log('Failed ');
                }
                else {
                    alert('Successfully reserve Data ');
                    console.log(result);
                }
            },
            error: function () {
                alert('Error occures to reserve the Data');
                console.log('Error occures ');
            }
        })
        */
        toggleModal();
    }

    function cancel(id) {
        let data = {
            id: id
        }
        $.ajax({
            type: 'POST',
            url: '/Appointment/DeleteAppointment',
            contentType: 'application/x-www-form-urlencoded; charset=UTF-8', // when we use .serialize() this generates the data in query string format. this needs the default contentType (default content type is: contentType: 'application/x-www-form-urlencoded; charset=UTF-8') so it is optional, you can remove it
            data: data,
            success: function (result) {
                if (result == "error") {
                    alert('Failed to reserve the Data');
                    console.log('Failed ');
                }
                else {
                    alert('Successfully reserve Data ');
                    console.log(result);
                }
            },
            error: function () {
                alert('Error occures to reserve the Data');
                console.log('Error occures ');
            }
        })
        toggleModal();
    }
</script>
